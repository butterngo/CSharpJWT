apiVersion: v1
kind: Service
metadata:
  name: {{ template "oauthserver.fullname" . }}
  labels:
    app: {{ template "oauthserver.name" . }}
    chart: {{ template "oauthserver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "oauthserver.name" . }}
    release: {{ .Release.Name }}
